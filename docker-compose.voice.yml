services:
  speaches:
    image: ghcr.io/speaches-ai/speaches:latest-cpu
    container_name: ollamachat-speaches
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - speaches-cache:/home/ubuntu/.cache
    # Uncomment for NVIDIA GPU passthrough (much faster Whisper inference):
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]

volumes:
  speaches-cache:
